# Example environment variables for local development
# Copy this to `.env` (do NOT commit real keys) and fill in values.

OPENAI_API_KEY=YOUR_OPENAI_API_KEY
WEATHER_API_KEY=YOUR_WEATHER_API_KEY
# Optional: path to sqlite database (defaults in config.py)
# DATABASE_PATH=./data/History1
